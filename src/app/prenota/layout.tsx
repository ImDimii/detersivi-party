import { Metadata } from "next"

export const metadata: Metadata = {
  title: "Prenota Allestimento o Consulenza | DetersiviParty",
  description: "Prenota online il tuo appuntamento per una consulenza personalizzata o l'allestimento del tuo prossimo evento speciale.",
}

export default function ReservationLayout({ children }: { children: React.ReactNode }) {
  return children
}
